Context: You are building validation experiments for a Kaggle hackathon project. The project is an offline-first clinical triage assistant using Google Gemma 4 E2B running on a Mac Mini M2 with 8GB RAM. The system reads photographed paper forms and voice notes, then outputs structured JSON with triage scores. All tests must run locally with no cloud dependencies. Use uv for Python environment management. Create a clean harness with isolated virtual environments. Each test must produce a clear PASS/FAIL verdict with evidence.

Local model runner: Ollama v0.21.1+ with native gemma4 architecture support. The model tag is `gemma4:e2b` (Gemma 4 E2B-it, Q4_K_M quantization, 7.2GB, 5.12B params, vision+text). Ollama runs at `http://127.0.0.1:11434` and uses the `/api/chat` endpoint. The harness sends images as base64 in the user message (before text, per Gemma 4 multimodal best practice). Sampling: temperature=0 for structured extraction, top_p=0.95, top_k=64, num_ctx=4096. Thinking mode is disabled (no <|think|> in system prompt). Target hardware is a Mac Mini M2 with 8GB RAM, so keep context windows and image sizes small. Latency and memory usage must be tracked.